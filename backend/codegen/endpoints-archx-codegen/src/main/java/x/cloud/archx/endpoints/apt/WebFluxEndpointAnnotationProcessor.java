package x.cloud.archx.endpoints.apt;

import javax.annotation.processing.SupportedAnnotationTypes;

@SupportedAnnotationTypes({"x.cloud.archx.endpoints.*"})
public class WebFluxEndpointAnnotationProcessor extends AbstractEndpointProcessor {
    
}
