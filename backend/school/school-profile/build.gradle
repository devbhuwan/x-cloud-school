apply plugin: 'org.springframework.boot'

dependencies {
    compile([
            libs.springBootStarterWeb
    ])

    compileOnly([

    ])

//        apt([
//
//        ])

    testCompile([
    ])
}