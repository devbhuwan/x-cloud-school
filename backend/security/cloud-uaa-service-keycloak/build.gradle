apply plugin: 'wildfly-swarm'

dependencies {
    compile([
            libs.keycloakServer
    ])
    compileOnly([

    ])

//        apt([
//
//        ])

    testCompile([
    ])
}

task runJar(type: Exec, dependsOn: [build]) {
    executable "java"
    args "-jar", "${buildDir}/libs/${project.name}-swarm.jar"
}