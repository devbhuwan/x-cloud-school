apply plugin: 'wildfly-swarm'
apply from: "$rootDir/gradle/docker.gradle"

dependencies {
    compile([
            libs.keycloakServer
    ])
    compileOnly([

    ])

//        apt([
//
//        ])

    testCompile([
    ])
}
