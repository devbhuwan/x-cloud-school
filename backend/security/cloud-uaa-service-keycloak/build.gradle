apply plugin: 'wildfly-swarm'

dependencies {
    compile([
            libs.keycloakServer
    ])

    compileOnly([

    ])

//        apt([
//
//        ])

    testCompile([
            libs.testNG
    ])
}