dependencies {
    compile([
            libs.springBootAutoConfigure
    ])

    compileOnly([
        libs.springBootConfigurationProcessor
    ])

//        apt([
//
//        ])

    testCompile([
    ])
}

compileJava.dependsOn(processResources)